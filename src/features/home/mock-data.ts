import type { HouseholdStatus } from './types';

// Local mock data only — no backend yet.

export const householdStatus: HouseholdStatus = {
  householdName: 'The Loft',
  greetingName: 'Myles',
  statusMessage: 'Everything looks good',
};
